[Unit]
Description=Check if fdroid.service is running and execute commands

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'if systemctl --user is-active --quiet fdroid.service; then podman exec --user 101:101 --workdir=/ fdroid python3 /download.py; podman exec --user 101:101 --workdir=/fdroid fdroid fdroid update; fi'